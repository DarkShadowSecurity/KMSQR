fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
# cryptography pinned to >= 46.0.7 to pick up:
#   GHSA-79v4-65xg-pq4g (CVE-2024-12797 OpenSSL bundled in wheels, fixed 44.0.1)
#   GHSA-r6ph-v2qm-q3c2 (SECT subgroup validation, fixed 46.0.5)
#   GHSA-m959-cc7f-wv43 (X.509 wildcard SAN bypass, fixed 46.0.6)
#   GHSA-p423-j2cm-9vmq (CVE-2026-39892 non-contiguous buffer overflow, fixed 46.0.7)
cryptography==48.0.0
argon2-cffi==25.1.0
slowapi==0.1.9
# SQLAlchemy Core is the storage abstraction (SQLite by default; PostgreSQL via
# PQKMS_DB_URL for HA). The PostgreSQL driver (psycopg) is an optional extra
# installed only when targeting Postgres (see requirements-ha.txt).
SQLAlchemy==2.0.48
# Prometheus metrics exposition for /metrics.
prometheus-client==0.21.1
# liboqs-python: post-quantum primitives. Version must match the C liboqs
# installed in the container image (see deploy/Dockerfile LIBOQS_REF).
# If unavailable at runtime, the KMS degrades to classical-only (X25519 +
# Ed25519) and tags material with CLASSIC_* suites so it's never confused
# with hybrid PQ material.
liboqs-python==0.14.1 ; platform_system != "Windows"
